const DEFAULT_CONFIG_NAME = 'badgeConfig.js'
const DEFAULT_STATE = {
  format: 'svg',
  color: 'lightgray',
  labelColor: 'gray',
  template: 'flat',
  text: [
    'boring',
    'badge',
  ],
}

module.exports = {
  DEFAULT_CONFIG_NAME,
  DEFAULT_STATE,
}
